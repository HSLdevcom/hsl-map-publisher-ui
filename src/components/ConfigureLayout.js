import React, { Component } from 'react';
import { observer } from 'mobx-react';

class ConfigureLayout extends Component {
  render() {
    return <div />;
  }
}

export default observer(ConfigureLayout);
